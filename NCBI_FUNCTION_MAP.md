# NCBI BLAST+ C → blast-rs Function Map

Systematic comparison of every function in the NCBI blastp path against its blast-rs equivalent.
Each function must match the C original or the difference must be documented.

## Upstream Source Layout

For ongoing audit, prefer the local BLAST+ 2.17.0 tarball-shaped source tree,
because its paths and file versions match the CCC reference:

`ncbi-blast-2.17.0+-src/c++/src/algo/blast/core/...`

For local navigation, this repo keeps an ignored symlink:

`.tmp/upstream-ncbi-core-tarball -> ../ncbi-blast-2.17.0+-src/c++/src/algo/blast/core`

The checked-out NCBI C++ Toolkit is also available as project-adjacent source:

`/home/mahogny/github/claude/ncbi-cxx-toolkit-rs/ncbi-cxx-toolkit-public`

That checkout comes from:

`https://github.com/ncbi/ncbi-cxx-toolkit-public.git`

The GitHub toolkit checkout has the same core source tree without the BLAST+
tarball packaging prefix:

`src/algo/blast/core/...`

`.tmp/upstream-ncbi-core -> /home/mahogny/github/claude/ncbi-cxx-toolkit-rs/ncbi-cxx-toolkit-public/src/algo/blast/core`

Use the GitHub checkout only when we intentionally want current toolkit source
rather than the exact BLAST+ 2.17.0 tarball snapshot.

## Status: CORE PARITY MAP IN PROGRESS

## NCBI C Functions (in call order for blastp)

### 1. Lookup Table Construction
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `BlastAaLookupTableNew` | blast_aalookup.c | `ProteinLookupTable::build()` | ✅ Matched (thick backbone + overflow) |
| `BlastAaLookupFinalize` | blast_aalookup.c | (inline in build) | ✅ Matched |
| `_ComputeIndex` / `ComputeTableIndex` | blast_lookup.h | `word_hash()` | ✅ Matched (shift-based) |
| `ComputeTableIndexIncremental` | blast_lookup.h | Inline in scan loop | ✅ Matched |
| PV construction | blast_aalookup.c | Inline in build / `merge_pv()` no-hit prefilter | ✅ Matched (32768 bits for word size 3; batch prefilter preserves two-hit scanner results) |
| `LookupTableWrapInit` / `LookupTableWrapInit_MT` | lookup_wrap.c | `lookup_table_wrap_init()` / `lookup_table_wrap_init_mt()` / `lookup_table_wrap_init_nucleotide()` | ✅ Matched Rust typed lookup ownership transfer with C-shaped represented variant dispatch, including standard Na and NaHash ownership/destructor/offset-size handling; represented nucleotide construction now estimates table entries, chooses the concrete lookup table, dispatches megablast/SmallNa/standard Na/NaHash constructors, preserves SmallNa fallback to standard Na, and threads `num_threads` through the NaHash construction boundary |
| `BlastChooseNaExtend` | na_ungapped.c | `blast_choose_na_extend()` | ✅ Matched represented nucleotide lookup and extension callback selection as typed enums instead of C raw function pointers, including standard Na direct/aligned/generic extension selection, SmallNa lookup-width inference from backbone size for non-direct mini-extension selection, and NaHash's NULL lookup/extension callback boundary |

### 2. Subject Scanning
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `s_BlastAaScanSubject` | blast_aascan.c | (inline in scan loop) | ✅ Matched |
| `s_BlastnDiagHashExtendInitialHit` | na_ungapped.c | `s_blastn_diag_hash_extend_initial_hit()` | ✅ Matched represented packed-nucleotide core: exact-word expansion, diagonal explored-area guard, ungapped extension, and HSP-save signal; mask, two-hit-window, and lookup-layout routing remain in scanner callers |
| `PV_TEST` macro | blast_extend.h | `pv[hash>>6] & (1<<(hash&63))` | ✅ Matched |

### 3. Two-Hit Word Finding
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `s_BlastAaWordFinder_TwoHit` | aa_ungapped.c:440 | `protein_scan_with_table_reuse()` | ✅ Matched |
| Diagonal array (`DiagStruct`) | blast_extend.h:57 | `diag_buf: Vec<(i32, bool)>` | ✅ Matched semantics |
| `diag_offset` trick | aa_ungapped.c:316 | `diag_offset = TWO_HIT_WINDOW` | ✅ Matched |
| Two-hit window check | aa_ungapped.c:373 | `diff >= TWO_HIT_WINDOW` | ✅ Matched |
| Word overlap check | aa_ungapped.c:384 | `diff < ws` | ✅ Matched |

### 4. Ungapped Extension
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `s_BlastAaExtendTwoHit` | aa_ungapped.c:1089 | `extend_left`/`extend_right` in scanner | ✅ Matched |
| Word-best-start scan | aa_ungapped.c:1108-1118 | Inline in scan loop | ✅ Matched |
| Left must reach first hit | aa_ungapped.c:1139 | `reached_first` check | ✅ Matched |
| Right extend with init_score | aa_ungapped.c:1147 | `extend_right(..., left_score)` | ✅ Matched |
| `s_BlastAaExtendLeft` | aa_ungapped.c:903 | `extend_left()` | ✅ Matched (`>=` xdrop) |
| `s_BlastAaExtendRight` | aa_ungapped.c:831 | `extend_right()` | ✅ Matched (`score <= 0`, `>=` xdrop, `s_last_off`) |
| `s_BlastAaExtendOneHit` | aa_ungapped.c:1071 | `protein_ungapped_extend()` | ✅ Matched for one-hit callers; two-hit path correctly uses `s_BlastAaExtendTwoHit` |

### 5. Constants & Parameters
| NCBI Constant | Value | blast-rs Value | Status |
|--------------|-------|---------------|--------|
| `BLAST_UNGAPPED_X_DROPOFF_PROT` | 7 bits | converted with ungapped lambda | ✅ Matched |
| `BLAST_GAP_X_DROPOFF_PROT` | 15 bits | 15 bits (converted to raw) | ✅ Matched |
| `BLAST_GAP_X_DROPOFF_FINAL_PROT` | 25 bits | 25 bits | ✅ Matched |
| `BLAST_GAP_TRIGGER_PROT` | 22 bits | converted with ungapped lambda/logK | ✅ Matched |
| `BLAST_WORD_THRESHOLD_BLASTP` | 11 | 11 | ✅ Matched |
| `AA_HITS_PER_CELL` | 3 | 3 | ✅ Matched |
| Two-hit window | 40 | 40 | ✅ Matched |
| charsize | 5 bits | 5 bits (CHARSIZE) | ✅ Matched |

### 6. Gapped Alignment
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `BlastGetStartForGappedAlignment` | blast_gapalign.c | `get_start_for_gapped_alignment()` | ✅ Matched |
| `MBSpaceNew` / `s_GetMBSpace` / `s_RefreshMBSpace` | greedy_align.c | `mbspace_new()` / `s_get_mbspace()` / `s_refresh_mbspace()` | ✅ Matched pool shape with Rust-owned chunk vector replacing C linked nodes |
| `s_BlastGreedyAlignMemAlloc` | blast_gapalign.c | `s_blast_greedy_align_mem_alloc()` | ✅ Matched allocation sizing for non-affine and affine greedy work buffers |
| `AdjustSubjectRange` | blast_gapalign.c | `adjust_subject_range()` | ✅ Matched long-subject traceback window narrowing, strict `< 90000` no-op threshold, no-shift branch, shifted-offset branch, and coordinate `start_shift` handoff |
| `BLAST_GappedAlignmentWithTraceback` | blast_gapalign.c | `protein_gapped_align()` | ✅ Matched for ALIGN_EX script concatenation and terminal-gap accounting |
| `s_ChainingAlignment` | blast_gapalign.c | `s_chaining_alignment()` | ✅ Matched chaining DP recurrence and seed-drop threshold over Rust `ProteinHit` seeds |
| `BLAST_GetGappedScore` | blast_gapalign.c | `blast_get_gapped_score()` | ✅ Matched ordinary protein seed loop: score ordering, preliminary interval-tree containment, start-point selection, preliminary gapped DP cutoff, traceback rerun, common-endpoint purge, and score sort |
| `Blast_TracebackFromHSPList` | blast_traceback.c | `blast_traceback_from_hsp_list()` | ✅ Matched ordinary protein traceback branch: score order, containment tree, final-xdrop traceback rerun, HSP field update, common-endpoint purge, and score sort |
| `BLAST_GetUngappedHSPList` | blast_gapalign.c | `blast_get_ungapped_hsp_list()` | ✅ Matched init-hit loop, query-context rebasing, HSP field mapping, list allocation/reuse, and score sort |
| `JumperExtendRightWithTraceback` | jumper.c | `jumper_extend_right_with_traceback()` | ✅ Matched direct-byte right extension traceback, raw jumper edit ops, recent-error trace window, trimming, identity count, and overflow-safe extension length outputs |
| `JumperPrelimEditBlock*` / `s_ResetJumperPrelimEditBlocks` / `s_GetSeqPositions` / `s_ComputeExtensionScore` | jumper.c | `jumper_prelim_edit_block_new()` / `jumper_prelim_edit_block_add()` / `s_reset_jumper_prelim_edit_blocks()` / `s_get_seq_positions()` / `s_compute_extension_score()` | ✅ Matched represented prelim block allocation/add/free, invalid op rejection, paired reset no-op behavior for missing halves, sequence-position walking through matches/mismatches/indels, exact-end walking, null/negative/out-of-range rejection, and gap-run extension scoring |
| `s_ShiftGaps` / `s_ShiftGapsRight` / `s_TrimExtension` | jumper.c | `s_shift_gaps()` / `s_shift_gaps_right()` / `s_trim_extension()` | ✅ Matched represented jumper edit normalization, opposite-indel collapse, flank-gap trimming, packed-subject bounds rejection, pointer arithmetic during extension trimming, zero-margin/empty no-ops, single-gap clearing, and missing-prelim-block rejection |
| `BlastNaExtendJumper` / `JumperGoodAlign` / `JumperFindEdits` / `s_CreateHSP` / `s_CreateHSPForWordHit` / `s_SaveSubjectOverhangs` | jumper.c | `blast_na_extend_jumper()` / `jumper_good_align()` / `jumper_find_edits()` / `s_create_hsp()` / `s_create_hsp_for_word_hit()` / `s_save_subject_overhangs()` | ✅ Matched represented mapper/short-read extension loop: invalid lookup metadata rejection, diagonal/query/subject ordering with overflow-safe invalid offset skips, exact pre-extension with malformed-subject rejection including discontiguous megablast extension using MB lookup width rather than scan template width, explored diagonal skip, invalid initial word-hit offset skips, truncated-query-context skip behavior, compressed Jumper traceback with malformed sequence-shape rejection and overflow-safe aligned-start recovery, `JumperGoodAlign` identity/score/edit-distance checks including zero-length rejection and formula cutoff routing, mapper `map_info` attachment on created HSPs, edit extraction with invalid-shape/op/endpoint rejection, word-hit HSP invalid-offset and malformed-subject rejection, full HSP invalid-range/query-shape/subject-shape rejection, missing-prelim-block HSP rejection, splice-flag-gated edge/overhang population, HSP save, optional direct exact and indexed left/right small-word subject rescue including wildcard query-base matching and overflow-safe overhang bounds, malformed saved-HSP/query/subject shape no-ops, overflow-safe intron windows, subject-bounds rejection, right/left ambiguous-edge trimming, subject-overhang invalid-input and overflow-safe terminal length handling, threshold no-ops, no-index-match no-op behavior, terminal overhang `blastn-short`/`rmblastn` subject/DB CLI parity including order-insensitive multi-subject content parity, poly-A trailing overhang `blastn-short`/`rmblastn` subject/DB CLI parity including order-insensitive multi-subject content parity, minus-strand terminal secondary-HSP rescue parity including order-insensitive multi-subject `blastn-short` scored and `rmblastn` coordinate/sequence/BTOP parity, adapter-like trailing query overhang `blastn-short`/`rmblastn` subject/DB CLI parity including order-insensitive multi-subject content parity, and exact adapter trimming `blastn-short`/`rmblastn` subject/DB CLI parity including same-subject-interval strand tie ordering and order-insensitive multi-subject content parity with ignored subject-mode exact-order repros |
| `SubjectIndexNew` / `SubjectIndexIterator*` | jumper.c | `subject_index_new()` / `subject_index_iterator_new()` / iterator next/prev/free helpers | ✅ Matched represented small-word subject index construction, invalid-shape and malformed packed-buffer rejection, overflow-safe packed-word/index bounds, lookup-window initialization, empty short-subject indexes, missing-word iterators, forward/reverse bounds, negative iterator indexes, and lifecycle no-op free |
| `JumperNaWordFinder` / `s_DetermineScanningOffsets` | na_ungapped.c / masksubj.inl | `jumper_na_word_finder()` / `jumper_na_word_finder_with_word_hits()` / `jumper_na_word_finder_with_subject_ranges()` / `s_determine_scanning_offsets()` | ✅ Matched represented contiguous lookup dispatch including scan-step-respecting standard Na, contiguous megablast, direct SmallNa backbone/overflow retrieval, and represented 16-base NaHash lookup-table retrieval through the C `FNV_hash` bucket callback, empty/too-short subject no-op, invalid word-length and invalid lookup metadata rejection, empty masked-range no-op, unsupported lookup rejection, packed-subject word bounds/overflow/malformed-buffer rejection including discontiguous scan inputs, direct extension branch, `MapperWordHits` batching, adjacent same-diagonal suppression with overflow-safe position deltas, overflow-safe malformed offset skips and scan skip-cursor arithmetic before batching arithmetic, flush-on-full, final bucket flush, malformed batching-state rejection including nonpositive bucket sizes, short count arrays, short pair-array buckets, and short per-context diagonal/position arrays, masked subject scan-range advancement including skipped short ranges, C's read-is-query continuation rule, per-range `s_range` extension boundaries, and overflow-safe extreme range endpoints, discontiguous megablast template scan routing, two-template secondary seed collection and core Jumper extension, C-shaped stats updates, deterministic NCBI fixtures for lowercase query masking on both strands plus subject-mode and DB-mode dc-megablast discontiguous/two-template CLI parity, including multi-subject ordering coverage, lowercase-masked discontiguous template subject/DB coverage, terminal, minus-strand terminal, poly-A trailing overhang, adapter-like trailing overhang, exact adapter trimming, and order-insensitive multi-subject terminal/minus-strand-terminal/adapter-like/poly-A/exact-adapter `blastn-short` subject/DB mapper coverage, terminal, poly-A trailing, adapter-like trailing, exact adapter trimming, order-insensitive multi-subject terminal/minus-strand-terminal/adapter-like/poly-A/exact-adapter, and minus-strand terminal coordinate/sequence/BTOP `rmblastn` subject/DB mapper coverage with ignored subject/DB repro fixtures, including multi-subject scored repros, for the remaining scored mismatch, ignored subject-mode exact-order repros for the multi-subject adapter tie, and NCBI secondary HSP preservation; broader mapper fixtures remain in TODO |
| `BlastMBLookupTableNew` / `EstimateNumTableEntries` | blast_nalookup.c / lookup_util.c | `blast_mb_lookup_table_new()` / `estimate_num_table_entries()` | ✅ Matched represented megablast constructor orchestration: C right-minus-left table-entry estimate, hash/PV sizing and PV compression bits, optional database word-count filtering, contiguous/discontiguous fill dispatch, discontiguous longest-chain estimation with separate two-template helper arrays and first-word adjustment, scan-step setup, and destructor-on-error ownership behavior for represented paths |
| `BlastSmallNaLookupTableNew` / `BlastNaLookupTableNew` | blast_nalookup.c | `blast_small_na_lookup_table_new()` / `blast_na_lookup_table_new()` | ✅ Matched represented constructor orchestration for SmallNa and standard Na: word/lut width validation, scan-step and mask/backbone metadata, exact-match query indexing, thin-backbone finalization into Rust-owned lookup tables, and SmallNa lookup-width inference from C's backbone-size rule for scanner/extension dispatch; Rust does not store C's optional inverted masked-location list |
| `BlastNaHashLookupTableNew` / `s_NaHashLookupScanSubjectForWordCounts` | blast_nalookup.c | `blast_na_hash_lookup_table_new()` / `s_na_hash_lookup_scan_subject_for_word_counts()` | ✅ Matched represented NaHash constructor orchestration: db-filter requires SeqSrc, word/lut width setup, scan-step rule, grow-on-set PV fill, poly-A word removal, sparse database word-count filtering, unique-word hash-width selection, FNV-backed exact-match indexing including C's leading lookup-width windows when full word size exceeds hash width, and finalize into thick-backbone/overflow layout; Rust does not allocate C's full 2^32-bit PV array and does not store inverted masked-location lists |
| `MB_IndexedWordFinder` | na_ungapped.c | `mb_indexed_word_finder()` / `mb_indexed_word_finder_with_fallback()` | ✅ Matched indexed callback ingestion with OID/chunk forwarding, non-indexed OID fallback hook with fallback-before-reset state preservation, zero word-size callback-hit no-extension/no-pass stats behavior, huge word-size diagonal-end saturation, zero-hit callback stats, malformed packed-subject rejection before extension, `ungapped_extension` gating, IR diagonal suppression with overflow-safe qend refreshes, invalid indexed-offset rejection, ungapped extension failure accounting, ungapped extension filtering including high-cutoff rejection, score sort for represented path, and real `makembindex` megablast hit, no-hit, and multi-subject ordering CLI parity through the ordinary Rust DB scan path; native MegaBLAST index reading remains unsupported |
| `ShortRead_IndexedWordFinder` | na_ungapped.c | `short_read_indexed_word_finder()` / `short_read_indexed_word_finder_with_fallback()` | ✅ Matched indexed callback ingestion with OID/chunk forwarding, non-indexed OID fallback hook with fallback-before-reset state preservation, zero word-size callback-hit no-HSP/no-pass stats behavior, huge word-size diagonal-end saturation, zero-hit callback skip behavior, malformed packed-subject skip behavior, missing-query-context and truncated-query-context skip behavior, IR diagonal suppression with overflow-safe qend refreshes, invalid indexed-offset failure accounting including shifted-context local-offset saturation, Jumper traceback, good-align HSP save/reject, inline indexed HSP creation without generic mapper gap shift or subject-overhang saving, gapped stats for represented path, deterministic DB-mode masked short-read parity, and real `makembindex` masked short-read hit, no-hit, and multi-subject ordering CLI parity through the ordinary Rust DB scan path; native MegaBLAST index reading remains unsupported |
| `DoAnchoredScan` | jumper.c | `do_anchored_scan()` | ✅ Matched represented anchored flank scan: big-word selection, query-word bounds/overflow and ambiguity rejection, invalid query/subject shape no-ops, overflow-safe scan-window length and scan-step checks, ambiguity/polyA and too-short-window skips with focused negative coverage, direct left/right flank scanning, packed subject word scan including no-match and malformed-subject empty behavior, short alignment-output scratch rejection, best Jumper traceback HSP selection, and list save |
| `DoAnchoredSearch` | jumper.c | `do_anchored_search()` / `do_anchored_search_to_stream()` | ✅ Matched represented partial-chain flank recovery, overflow-safe intron flank endpoints, subject/query starts, caller-word-size gating for opposite-flank scans, and trailing-query checks, original chain HSP copy on recovered flank including multi-HSP saved chains and multiple linked saved chains for the selected subject after score-floor filtering, C-shaped stream write/status wrapper including linked saved-chain output for default/nonzero query indexes and insert-error propagation for out-of-range query indexes, query-index selection/defaulting for stream writes including negative-context clamping, missing-stream error, missing mapper-data error, empty-search success for both entry shapes, empty saved-chain skip, selected-chain skip for missing/out-of-range query contexts, and focused stream-output tests; broader mapper fixtures remain in TODO |
| `HSPContainerNew` / `HSPContainerDup` / `HSPChainNew` / `CloneChain` / lifecycle frees | spliced_hits.c | `hspcontainer_new()` / `hspcontainer_dup()` / `hspchain_new()` / `clone_chain()` / free wrappers | ✅ Matched represented ownership transfer, recursive duplication, default chain fields, null-input no-ops, pair-link clearing on chain free, and result/free lifecycle shape |
| `FindPartialyCoveredQueries` | hspfilter_mapper.c | `find_partialy_covered_queries()` | ✅ Matched saved-chain scan and clone rules for same OID, score floor, leading uncovered query, trailing uncovered query, and overflow-safe malformed trailing endpoints |
| `s_FindSpliceJunctions` / `s_IntronToGap` / `s_FindSpliceJunctionsForGap` / `s_FindSpliceJunctionsForOverlaps` / `JumperFindSpliceSignals` | hspfilter_mapper.c / jumper.c | `s_find_splice_junctions()` / `s_intron_to_gap()` / `s_find_splice_junctions_for_gap()` / `s_find_splice_junctions_for_gap_from_map_info()` / `s_find_splice_junctions_for_overlaps()` / `jumper_find_splice_signals()` | ✅ Matched represented mapper splice-signal search, terminal exon edge marking, interior edge-base capture, invalid shape/range rejection, overhang-backed gap extension with overflow-safe gap/window and short-intron gate arithmetic, overflow-safe overlap split trimming, short-intron merge, required-state rejection, stale splice-edge clearing on no-signal gaps, empty-chain score recomputation, and mapper splice-edge bit updates; broader upstream mapper fixtures remain in TODO |
| `s_TrimChainStartToSubjPos` / `s_TrimChainEndToSubjPos` | hspfilter_mapper.c | `s_trim_chain_start_to_subj_pos()` / `s_trim_chain_end_to_subj_pos()` | ✅ Matched represented chain trimming, score refresh, containment drop, and mapper splice/exon edge-bit clearing |
| `s_SetAdapter` / `s_FindAdapterInSequence` / `s_FindAdapters` / `s_SetPolyATail` / `s_FindPolyATails` | hspfilter_mapper.c | `s_set_adapter()` / `s_find_adapter_in_sequence()` / `s_find_adapters()` / `s_set_poly_a_tail()` / `s_find_poly_a_tails()` | ✅ Matched represented chain trimming/position propagation, invalid-input state preservation, adapter invalid-range rejection, missing/short-query and full-coverage no-op branches, overflow-safe query-endpoint, terminal-skip, and trim arithmetic, poly-A skip behavior, C-shaped mapper adapter/poly-A/exon edge-bit side effects, and finalizer-level adapter/poly-A pass coverage |
| `s_MergeHSPs` | hspfilter_mapper.c | `s_merge_hsps()` | ✅ Matched represented extent/gap-script merge plus overflow-safe fallback span/gap accounting, mapper edit append, right-edge transfer, right-overhang replacement, overlap rejection, and missing-scoring-parameter rejection |
| `s_TrimHSP` | hspfilter_mapper.c | `s_trim_hsp()` | ✅ Matched represented gap-script/coordinate trim plus overflow-safe score refresh, mapper edit pruning/position accounting, subject-overhang reconstruction, no-op preservation, and oversized-trim rejection without state mutation |
| `s_BlastHSPMapperSplicedPairedRun` / `s_FindBestPairs` / `s_FindRearrangedPairs` | hspfilter_mapper.c | `s_blast_hspmapper_spliced_paired_run()` / `s_find_best_pairs()` / `s_find_rearranged_pairs()` | ✅ Matched represented paired-read grouping via `BlastContextInfo.segment_flags == eFirstSegment`, mapper cutoff-function routing, duplicate/invalid/out-of-range HSP skip behavior, context-frame assignment for generated chains, multi-HSP splice-candidate grouping before saved-chain insertion, paired-run splice merging coverage, best-pair selection including convergent trim, parallel same-strand pair marking, cutoff no-candidate preservation, and required-state rejection including non-even query positions; Rust stores pair indexes/cached scores instead of raw `HSPChain*` peer pointers |
| `s_BlastHSPMapperFinal` | hspfilter_mapper.c | `s_blast_hspmapper_final()` / `s_finalize()` | ✅ Matched represented finalizer flow, required-state rejection, pruning, adapter/poly-A/rearranged-pair passes including finalizer-level adapter trimming and poly-A marking coverage, OID/fragment sort, optional overlap splitting including empty/no-HSP list edges, filtering, unique-count assignment including duplicate-fragment grouping, and saved-chain transfer |
| `BlastHSPMapperParamsNew` / `s_BlastHSPMapperPairedInit` / `s_BlastHSPMapperPairedNew` / `s_ComputeChainScore` / `s_SortChains` | hspfilter_mapper.c | `blast_hspmapper_params_new()` / `s_blast_hspmapper_paired_init()` / `s_blast_hspmapper_paired_new()` / `s_compute_chain_score()` / `s_sort_chains()` | ✅ Matched represented params/writer callback/data shape including missing optional inputs, saved-chain slot reset including negative counts, HSP/gap-script chain scoring with missing-input, empty-chain rejection/zeroing edges, and overflow-safe gap arithmetic, and per-query linked-list score sorting with empty-slot no-op behavior |
| `s_RemoveOverlaps` / `s_GetOverlapCost` / `s_TrimOverlap` | hspfilter_mapper.c | `s_remove_overlaps()` / `s_get_overlap_cost()` / `s_trim_overlap()` | ✅ Matched represented adjacent query-overlap splitting, generated-chain pair clearing, non-overlap no-op, invalid-input rejection, and overflow-safe overlap-cost/trim arithmetic |
| `s_PruneChains` | hspfilter_mapper.c | `s_prune_chains()` | ✅ Matched represented two-pass pruning, score multiplicity assignment, pair bonus handling, empty/out-of-range query-count tolerance, and `HSPChainFree`-style mate-link clearing when a partner chain is pruned |
| `s_FilterChains` / `s_TestCutoffs` / `s_TestHSPRanges` / `s_FindFragmentStart` / comparators | hspfilter_mapper.c | `s_filter_chains()` / `s_test_cutoffs()` / `s_test_hsp_ranges()` / `s_find_fragment_start()` / comparator helpers | ✅ Matched represented score cutoff, overflow-safe edit-distance cutoff accounting, null/empty-list no-op, linked-list retention order, overflow-safe HSP range/edit-script validation, overflow-safe fragment-start frame rules, and score/OID/start comparator ties |
| `HSPChainListInsert` / `HSPChainListTrim` / `s_TestChains` | hspfilter_mapper.c | `hspchain_list_insert()` / `hspchain_list_trim()` / `s_test_chains()` / `s_test_chains_sorted()` | ✅ Matched represented old/new insertion routing, duplicate suppression, cutoff-rejected consume/no-op behavior, score-margin trimming, empty-trim rejection, sortedness/context/order validation, and empty incoming handling |
| `s_HSPNodeArrayCopy` / `HSPPathNew` / `HSPPathFree` | hspfilter_mapper.c | `s_hspnode_array_copy()` / `hsppath_new()` / `hsppath_free()` | ✅ Matched represented path-node copy, zero-count no-op, negative/short-buffer/bad-link rejection, and path lifecycle shape |
| Gapped cutoff (trigger) | blast_parameters.c | per-query converted raw trigger | ✅ Matched |

### 6a. PHI-BLAST Pattern Search / Gapping
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `FindPatternHits` | pattern.c | `find_pattern_hits()` | ✅ Matched low-level PHI pattern scan helper |
| `PHIBlastScanSubject` | phi_lookup.c | `phi_blast_scan_subject()` | ✅ Matched scan shape; Rust returns typed `PhiInitialHit` subject bounds |
| `s_PHISaveInitialHit` | phi_extend.c | `s_phi_save_initial_hit()` | ✅ Matched C save order (`s_start`, `s_end`) via `PhiInitialHit` view |
| `PHIBlastWordFinder` | phi_extend.c | `phi_blast_word_finder()` | ✅ Matched scan/save/stats loop over subject pattern hits |
| `s_PHIGappedAlignment` | phi_gapalign.c | `s_phi_gapped_alignment()` | ✅ Matched score-only PHI gapping around the pattern hit |
| `PHIGetGappedScore` | phi_gapalign.c | `phi_get_gapped_score()` | ✅ Matched driver shape over explicit Rust PHI initial-hit view |
| `s_PHIBlastAlignPatterns` | phi_gapalign.c | `s_phi_blast_align_patterns()` | ✅ Matched PHI pattern edit-script construction using translated pattern-bound helpers and `s_banded_align()` |
| `PHIGappedAlignmentWithTraceback` | phi_gapalign.c | `phi_gapped_alignment_with_traceback()` | ✅ Matched wrapper flow; PHI flanks now use translated `ALIGN_EX`-style affine traceback with generic score matrix |
| `Blast_HSPUpdateWithTraceback` | blast_traceback.c | `blast_hsp_update_with_traceback()` | ✅ Matched ownership transfer of `edit_script` from gap workspace to HSP |
| `s_AdjustSubjectForTranslatedSraSearch` | blast_engine.c:1207 | `s_adjust_subject_for_translated_sra_search()` | ✅ Matched translated SRA subject-frame rotation and coordinate fixups for positive and negative frames |
| `s_PHITracebackFromHSPList` / `PHIBlast_HSPResultsSplit` | blast_traceback.c / blast_hits.c | `s_phi_traceback_from_hsp_list()` / `phiblast_hsp_results_split()` | ✅ Matched PHI list loop, traceback update/drop, score sort, PHI e-values, e-value reap, PHI bit scores, pattern-index result splitting, empty-source split behavior, and skipping HSPs without usable pattern metadata |
| `BLAST_ComputeTraceback` / `Blast_RunTracebackSearchWithInterrupt` | blast_traceback.c | `blast_compute_traceback()` / `blast_run_traceback_search_with_interrupt()` / `blast_traceback_dispatch_kind()` | ✅ Matched represented stream orchestration: invalid required-input rejection in both public traceback entry points including wrapper-level pre-close missing-query rejection, progress stage, preliminary close handoff including ordinary close and CBS close trimming before traceback callbacks, batch drain and HSP-stream batch-array edge behavior including public empty-stream success, per-list callback routing, result insertion including empty-list no-op, callback-emptied-list skip, bad-query-index rejection including context-derived out-of-range query indexes, same-OID batch collation by query context, and insert-error close, C-ordered traceback stream close, error-path close, interruption including per-batch interrupt-before-callback behavior, traceback SeqSrc encoding selection, translated-subject target view setup including partial missing-subject rejection, mask/filter/sort/prune policy including driver-level masklevel application, max-HSP/query-coverage filtering with missing-context query-coverage reaping plus direct no-op/missing-context filter edges, extended total-HSP-limit no-op flag clearing, no-flags exceeded reporting, short flag-slice tolerance, database-search-only e-value sort before `hitlist_size` pruning, `num_threads=2` DB gapped traceback CLI parity for nucleotide and translated thread-local subject handling, and top-level RPS/redo/PHI/ordinary dispatch order |

### 7. E-value / Statistics
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `BLAST_ComputeSearchSpace` | blast_stat.c | `compute_search_space()` | ✅ Matched |
| `BLAST_KarlinBlkCalc` | blast_stat.c | `lookup_protein_params()` | ✅ Matched |
| `BlastRawScore_ToEvalue` | blast_stat.c | `raw_to_evalue()` | ✅ Matched |
| `BlastHspNumMax` / `Blast_HitListUpdate` / `Blast_HitListMerge` / `Blast_HSPResultsInsertHSPList` / `Blast_HSPListsMerge` | blast_hits.c | `blast_hsp_num_max()` / `HitList::blast_hit_list_update()` / `blast_hit_list_merge()` / `blast_hsp_results_insert_hsp_list()` / `blast_hsp_lists_merge()` | ✅ Matched represented hit-limit and hit-list update rules: positive `hsp_num_max` applies independent of gapped mode, non-positive means unlimited, calloc-shaped hit-list summaries start `worst_evalue` at zero and update it during append, full hit-list heap replacement sorts incoming/existing HSP lists by e-value before refreshing `best_evalue`, `worst_evalue`, and `low_score`, tied heap-root comparisons replace with the newer list, result insertion creates new per-query hit lists with the caller's `hitlist_size`, internal hit-list purge truncates from the first empty list, empty incoming HSP-list merge preserves old-list ownership, HSP-list append/merge uses `hsp_num_max` as a temporary cap without mutating `hsp_max`, and hit-list merge rebuilds into a fresh destination with the old hit-list capacity, using split-query HSP merging only when C's scan finds a positive split offset and otherwise appending same-OID lists |
| `Blast_HSPListSubjectBestHit` / `BlastHSPSubjectBestHitOptionsNew` / `BlastHSPFilteringOptions_AddSubjectBestHit` | blast_hits.c / blast_options.c | `blast_hsp_list_subject_best_hit()` / `blast_hsp_subject_best_hit_options_new()` / `blast_hspfiltering_options_add_subject_best_hit()` | ✅ Matched represented subject-best-hit pruning, defaults, and option transfer: score-ordered later HSPs contained in an expanded better query range are removed, PHI lists are no-ops, BLASTN repeats the containment check against the opposite strand context using C's flipped query-coordinate rule, both protein and nucleotide defaults use max range diff 3, and `s_filter_blast_results()` runs the branch when subject-best-hit options are present |
| `Blast_TrimHSPListByMaxHsps` | blast_hits.c | `blast_trim_hsp_list_by_max_hsps()` | ✅ Matched C's max-HSP truncation side effect: the vector is shortened to `max_hsps_per_subject`, but cached `best_evalue` is intentionally left unchanged instead of recomputing from the remaining HSPs |
| `Blast_HSPListGetEvalues` | blast_hits.c | `blast_hsp_list_get_evalues()` | ✅ Matched represented C path: context Karlin selection, RPS preliminary fallback, gap decay, scaling factor, Spouge/Karlin branch, and `round_down`; simplified callers use `blast_hsp_list_get_evalues_simple()` |
| Bit-to-raw conversion | blast_parameters.c | implemented per program path | ✅ Matched |

### 8. Composition / PSI Kappa Helpers
| NCBI C Function | File | blast-rs Equivalent | Status |
|----------------|------|-------------------|--------|
| `s_GetStartFreqRatios` | blast_kappa.c | `get_start_freq_ratios()` | ✅ Matched |
| `s_GetPosBasedStartFreqRatios` | blast_kappa.c | `get_pos_based_start_freq_ratios()` | ✅ Matched |
| `s_ScalePosMatrix` | blast_kappa.c | `scale_pos_matrix()` / `psi_private_update_lambda_statistics()` | ✅ Matched boundary (position rows, private scaled PSI scores, and Lambda-ratio stat update hook) |
| `Kappa_posSearchItemsNew` / `Kappa_posSearchItemsFree` | blast_posit.c | `kappa_pos_search_items_new()` / `kappa_pos_search_items_free()` | ✅ Matched Rust-owned lifecycle and matrix/frequency payload |
| `Kappa_compactSearchItemsNew` / `Kappa_compactSearchItemsFree` | blast_posit.c | `kappa_compact_search_items_new()` / `kappa_compact_search_items_free()` | ✅ Matched Rust-owned compact search payload over `BlastScoreBlk` |
| `fillSfp` | blast_posit.c | `fill_sfp()` | ✅ Matched true-residue score-frequency construction |
| `impalaScaleMatrix` / `Kappa_impalaScaling` | blast_posit.c | `impala_scale_matrix()` / `kappa_impala_scaling()` | ✅ Matched PSI private/public matrix scaling and lambda recomputation boundary |
| `_PSIComputeScoreProbabilities` / `_PSIUpdateLambdaK` | blast_psi_priv.c | `psi_compute_score_probabilities()` / `psi_update_lambda_k()` | ✅ Matched PSSM score-frequency, ungapped Karlin update, and gapped PSI K derivation |
| `s_PSIDiscardIfUnused` / `_PSIPurgeAlignedRegion` | blast_psi_priv.c | `s_psi_discard_if_unused()` / `psi_purge_aligned_region()` | ✅ Matched aligned-region clearing and sequence-use discard rule |
| `s_fillColumnProbabilities` | blast_psi_priv.c | `s_fill_column_probabilities()` | ✅ Matched NCBIstdaa-to-effective-alphabet column probability ordering |
| `_PSISaveDiagnostics` / `_PSISaveCDDiagnostics` | blast_psi_priv.c | `psi_save_diagnostics()` / `psi_save_cd_diagnostics()` | ✅ Matched requested diagnostics copy-out, information-content calculation, bad-input/dimension rejection, CD-MSA shape rejection for CD diagnostics, short requested source/response-buffer rejection, unrequested short-source tolerance, and no-payload diagnostics response success |
| `_PSIStructureGroupCustomization` | blast_psi_priv.c | `psi_structure_group_customization()` | ✅ Matched query-row discard and position-count refresh |
| `_PSIPurgeBiasedSegments` / `s_PSIPurgeSelfHits` / `s_PSIPurgeSimilarAlignments` | blast_psi_priv.c | `psi_purge_biased_segments()` / `s_psi_purge_self_hits()` / `s_psi_purge_similar_alignments()` | ✅ Matched packed-MSA purge FSM, self-hit pass, and near-identical pass |
| `_PSIComputeAlignmentBlocks` / `_PSIGetLeftExtents` / `_PSIGetRightExtents` / `_PSIComputeAlignedRegionLengths` | blast_psi_priv.c | `psi_compute_alignment_blocks()` / `psi_get_left_extents()` / `psi_get_right_extents()` / `psi_compute_aligned_region_lengths()` | ✅ Matched extent propagation, X-adjusted aligned-region lengths, and malformed buffer-shape rejection |
| `_PSIComputeSequenceWeights` / `_PSICalculateNormalizedSequenceWeights` / `_PSICalculateMatchWeights` / `_PSISpreadGapWeights` | blast_psi_priv.c | `psi_compute_sequence_weights()` / `psi_calculate_normalized_sequence_weights()` / `psi_calculate_match_weights()` / `psi_spread_gap_weights()` | ✅ Matched Henikoff-style normalized weights, match-weight accumulation, gap dispersion, and malformed buffer-shape rejection at the sequence-weight entry point |
| `_PSICheckSequenceWeights` | blast_psi_priv.c | `psi_check_sequence_weights()` | ✅ Matched column sum validation, matching-sequence skip rule, query-X skip rule, and NSG compatibility threshold |
| `_PSIComputeFrequenciesFromCDs` / `s_PSIComputeFrequenciesFromCDsCleanup` | blast_psi_priv.c | `psi_compute_frequencies_from_cds()` / `s_psi_compute_frequencies_from_cds_cleanup()` | ✅ Matched CD weighted-frequency accumulation, query-residue inclusion, normalization, zero-sequence no-op success, missing-dimension/profile rejection, malformed buffer-shape rejection, and observation cap |
| `_PSIScaleMatrix` | blast_psi_priv.c | `psi_scale_matrix()` | ✅ Matched scaled-PSSM application, Lambda/K update, bracketing, binary-search scaling loop, and malformed buffer-shape rejection |
| `_PSIComputeFreqRatios` | blast_psi_priv.c | `psi_compute_freq_ratios()` | ✅ Matched pseudocount frequency-ratio blending, X-position zeroing, unaligned-column handling, and malformed buffer-shape rejection |
| `_PSIComputeFreqRatiosFromCDs` / `_PSIConvertFreqRatiosToPSSM` / `_PSICreateAndScalePssmFromFrequencyRatios` | blast_psi_priv.c | `psi_compute_freq_ratios_from_cds()` / `psi_convert_freq_ratios_to_pssm()` / `psi_create_and_scale_pssm_from_frequency_ratios()` | ✅ Matched CD frequency-ratio blending with matrix ratios, pseudocount handling, X-position zeroing, malformed buffer-shape rejection, direct frequency-ratio-to-PSSM shape validation, and create-and-scale conversion/scaling hard-error propagation |
| `s_PSISavePssm` | blast_psi.c | `s_psi_save_pssm()` | ✅ Matched internal PSSM matrix copy plus gapped/ungapped PSI Karlin field copy into `PSIMatrix`, including missing-output and missing-stat rejection |
| `s_WithDistinctEnds` | redo_alignment.c | `with_distinct_ends()` | ✅ Matched |
| `s_WindowsFromProteinAligns` | redo_alignment.c | `windows_from_protein_aligns()` | ✅ Matched |
| `s_WindowsFromTranslatedAligns` | redo_alignment.c | `windows_from_translated_aligns()` | ✅ Matched |
| `s_WindowsFromAligns` | redo_alignment.c | `windows_from_aligns()` | ✅ Matched |
| `s_DistinctAlignmentsSort` | redo_alignment.c | `distinct_alignments_sort()` | ✅ Matched |
| `s_GetComposition` / `Blast_GetCompositionRange` | redo_alignment.c / composition_adjustment.c | `get_composition()` / `get_composition_range()` | ✅ Matched |
| `Blast_AdjustScores` scale/RE branches | composition_adjustment.c | `blast_adjust_scores_with_workspace()` / `blast_adjust_scores_scale_old_matrix()` / `blast_adjust_position_based_scores()` | ✅ Matched for square scale-old, RE optimization, p-value test, and PSSM scale-old |
| `s_EvalueFromScore` | redo_alignment.c | `evalue_from_score()` | ✅ Matched |
| `s_preliminaryTestNearIdentical` | redo_alignment.c | `preliminary_test_near_identical()` | ✅ Matched |
| `Blast_RedoAlignCallbacks` | redo_alignment.h | `BlastRedoAlignCallbacks` + callback type aliases | ✅ Matched API surface |
| `Blast_RedoOneMatch` | redo_alignment.c | `blast_redo_one_match_with_callbacks_and_adjustment()` / `blast_redo_one_match_with_callbacks_and_position_adjustment()` / `blast_redo_one_match_in_memory()` / `blast_redo_one_match_in_memory_with_adjustment()` / `blast_redo_one_match_in_memory_with_position_adjustment()` / wrappers | ✅ Matched for callback no-composition, callback non-position composition-adjusted, callback position-based/PSSM ordinary, in-memory no-composition BLASTN/BLASTP/BLASTX/TBLASTN/TBLASTX, in-memory non-position protein-space including BLASTX/TBLASTX translated protein-space, and position-based/PSSM ordinary redo |
| `Blast_RedoOneMatchSmithWaterman` significance gate | redo_alignment.c | `smith_waterman_alignment_is_significant()` | ✅ Matched |
| `s_SmithWatermanScoreOnly` | blast_sw.c | `s_smith_waterman_score_only()` | ✅ Matched traceback-stage protein/PSSM score-only DP, including C's non-PSSM sequence swap |
| `s_GetTraceback` / `SmithWatermanScoreWithTraceback` | blast_sw.c | `s_get_traceback()` / `smith_waterman_score_with_traceback()` | ✅ Matched traceback byte layout, path bookkeeping, gap-open flags, non-PSSM swap restoration, PSSM no-swap behavior, and subject offset adjustment; Rust returns recovered hits instead of mutating `BlastHSPList` |
| `BLAST_SmithWatermanGetGappedScore` | blast_sw.c | `blast_smith_waterman_get_gapped_score()` | ✅ Matched score-only SW driver shape: ignores initial hits, scans valid contexts, applies RPS/per-context cutoffs, saves placeholder HSPs for traceback, and handles protein/PSSM plus packed nucleotide subjects |
| `s_NuclSmithWaterman` | blast_sw.c | `s_nucl_smith_waterman()` | ✅ Matched packed NCBI2na subject score-only DP |
| `Blast_RedoOneMatchSmithWaterman` | redo_alignment.c | `blast_redo_one_match_smith_waterman_with_callbacks_and_adjustment()` / `blast_redo_one_match_smith_waterman_with_callbacks_and_position_adjustment()` / `blast_redo_one_match_smith_waterman_with_callbacks()` / `blast_redo_one_match_smith_waterman_in_memory_nucl()` / `blast_redo_one_match_smith_waterman_in_memory_protein()` / `blast_redo_one_match_smith_waterman_in_memory_protein_with_adjustment()` / `blast_redo_one_match_smith_waterman_in_memory_protein_position_adjustment()` | ✅ Matched for callback no-composition, callback non-position composition-adjusted, callback position-based/PSSM, in-memory nucleotide/protein no-composition, materialized protein-space, BLASTX query-translated, TBLASTN translated-subject, TBLASTX query+subject-translated composition-adjusted, and position-based/PSSM score/start/final-X-drop paths; BLASTN/nucleotide composition-adjusted Smith-Waterman is intentionally rejected at the protein-space adjustment boundary |
| `Blast_RedoAlignmentCore_MT` materialized-subject path | blast_kappa.c | `blast_redo_alignment_core_mt_in_memory_subject()` / `blast_redo_alignment_core_mt_in_memory_subjects()` / `blast_redo_alignment_core_mt_seqsrc_subjects()` / `blast_redo_alignment_core_mt_with_callbacks()` / `merge_compo_thread_heaps()` / `sthread_local_data_array_setup()` | ✅ Matched for materialized, stream, SeqSrc, and callback-backed ordinary/SW redo branches currently represented in Rust, including per-context routing, heap retention/merge, early termination, program-specific SeqSrc encoding, supplied-context sum-stat/link-HSP evaluation, thread-local setup/trim invalid and no-op cases, BLASTN composition-adjusted Smith-Waterman rejection, and PSI private Lambda-ratio stat update hook |
| `BlastCompo_EarlyTermination` | redo_alignment.c | `blast_compo_early_termination()` | ✅ Matched |
| `BlastCompo_AlignmentsFree` | redo_alignment.c | `alignments_free()` | ✅ Matched |
| `s_SequenceDataRelease` | redo_alignment.c | `sequence_data_release()` | ✅ Matched |
| `s_WindowInfoFree` | redo_alignment.c | `window_info_free()` | ✅ Matched |

## Status

CCC currently reports no partial/stub pairs in the tracked core set.

Represented but still incomplete audit areas:

| Upstream Function | Source | Current Rust Coverage | Remaining Gap |
|------------------|--------|-----------------------|--------|
| `s_RPSGapAlignDataPrepare` / `s_RPSFillFreqRatiosInPsiMatrix` | blast_traceback.c:944 / blast_traceback.c:1001 | Represented by `s_rps_gap_align_data_prepare` and `s_rps_fill_freq_ratios_in_psi_matrix`; builds concatenated profile contexts and owned PSSM/frequency row maps with C alphabet-size selection for legacy and 28-column profile formats, including zero-length invalid profile contexts, absent frequency-ratio headers, C-shaped integer-to-floating frequency-ratio conversion, BLASTAA zero padding, and malformed owned profile-view rejection for bad magic, mismatched frequency/profile offsets, offsets/value shapes, negative profile counts, and negative starts | Uses an owned Rust profile view rather than raw mmapped row pointers attached to `BlastScoreBlk::psi_matrix` |
| `s_RPSComputeTraceback` | blast_traceback.c:1058 | Represented by `s_rps_compute_traceback`, `s_rps_compute_traceback_with_composition`, `s_rps_fetch_consensus_sequence`, and `s_rps_profile_pssm_for_traceback`; drains HSP stream including empty-stream success, rejects missing required inputs including absent RPS profile payloads and malformed profile/frequency data before stream drain, selects/skips RPS profile rows/frequency ratios by profile context including invalid zero-length contexts and direct invalid-context profile-PSSM rejection, fetches/skips RPS-BLAST/RPS-TBLASTN consensus sequences with PSSM-subject traceback encoding, prepares CBS scratch PSSMs, non-CBS RPS-BLAST `RPSRescalePssm` rows, and non-CBS RPS-TBLASTN direct profile rows with selected-row and direct malformed-row shape rejection, applies optional RPS Karlin K multiplier when metadata is present including the C RPS-TBLASTN CBS-only gate, routes traceback callback, restores RPS orientation, skips post-traceback empty lists, inserts results, applies final e-value sorting including the C interrupted-partial-results path, and closes traceback stream on callback errors, result-insert errors, or interrupts | Composition-based redo-alignment plumbing and direct `Blast_RedoAlignmentCore`/`Blast_TracebackFromHSPList` parity still need fixture integration |
| `PSICreatePssmWithDiagnostics` / `s_PSISavePssm` | blast_psi.c:105 / blast_psi.c:440 | Represented by `psi_create_pssm_with_diagnostics` and `s_psi_save_pssm`; orchestrates packed MSA, purge, internal allocation, optional structure-group customization, validation including malformed public MSA shapes, encoded residue range checks, and public-constructor `ignore_unaligned_positions` diagnostics coverage plus query-gap rejection, malformed converted MSA/CD-MSA shapes, CD-MSA bad-profile shapes, and unaligned CD-MSA cells ignoring absent or malformed profile payloads, alignment blocks, sequence weights, frequency ratios, PSSM conversion/scaling, save-out including final matrix/row/Karlin-stat rejection, optional diagnostics save-out only when request and diagnostics output are both supplied, focused public success/bad-MSA/diagnostics-payload/no-payload/bad-diagnostics tests including requested independent-observation allocation/free, diagnostics output supplied without request being cleared/ignored on success, required-input output preservation, missing-output-pointer rejection, post-allocation cleanup on frequency-ratio, bad-alphabet-size, and scaling hard errors, PSI-BLAST save-PSSM flag/output-path CLI constraint parity including no-save output paths accepted without artifact writes in subject/DB mode for combined and checkpoint-only/ASCII-only outputs, save-after-last non-empty checkpoint/ASCII artifact creation in subject/DB mode for combined and checkpoint-only/ASCII-only outputs, and save-each single-round no-artifact subject/DB CLI parity with focused intermediate-only round-file coverage, and PSI-BLAST `-in_msa` restart subject/DB CLI parity for default and explicit master-row query selection, first-description-token restart query IDs with full-defline residue-warning labels including whitespace-prefixed deflines, tab-containing deflines, and pipe-containing description tokens, master-gap query projection, empty non-master rows, parseable unselected pipe IDs, whitespace-tolerant sequence lines, CRLF line endings, blank record separators, and comment lines after blank separators, ambiguous alphabetic residues including the NCBI `O`-to-`X` score-calculation warning with gap-stripped/lowercase position handling, `ignore_msa_master`, and restart input incompatibility order for `query`/`in_msa`/`in_pssm` plus `msa_master_idx`/`ignore_msa_master` combinations, plus empty-MSA/out-of-range-master, all-gap empty-query master, single-row MSA rejection, non-positive `msa_master_idx`, non-FASTA, unequal-row, dot-residue, invalid sequence-character including form-feed whitespace, stop-residue, selected-master identifier-pipe, leading-blank, intra-record blank-continuation, pre-sequence and internal comment-line including repeated post-blank comments, empty-defline restart-MSA error parity, `num_iterations=0` convergence now running iterative rounds instead of zero output with ordinary first-round tabular hits, preserved iteration order, result-stream convergence marker, PSSM composition-statistics warning parity, and active output-shape parity coverage, and permissive `gap_trigger=-1` subject-mode output parity without forcing iterative PSI mode, with an ignored subject-mode repro fixture for remaining `num_iterations=0` final-row score/statistic mismatch | Needs broader upstream bad-MSA fixture parity, allocation-shaped hard error paths, and PSI iteration-output parity for numeric edges such as `num_iterations=0` convergence final-row statistics |
| `s_OutOfFrameAlignWithTraceback` | blast_gapalign.c:1334 | Represented by `s_out_of_frame_align_with_traceback`; preserves C-shaped inputs, offset outputs, forward `B -= 2` and reversed subject-letter pointer rules, optional prelim traceback block output, OOF edit-op space, the three-frame OOF score-state recurrence, named C frame-shift op codes, represented C gap-open/gap-extension traceback flags for both axes, frame-transition tie coverage, reversed ordinary-matrix query-row selection, translated subject/DB reverse-frame coordinate parity for BLASTX/TBLASTN/TBLASTX, BLASTX/TBLASTN/TBLASTX subject/DB indel rendering parity for translated traceback paths that remain ungapped including TBLASTX one-base frame-offset insertions and translated frame-shift-pattern alignments across multiple frames, BLASTX/TBLASTN subject/DB one-base frame-shift rendered gap-script/BTOP parity, active TBLASTX complex frame-shift top-HSP subject-mode and DB-mode rendering parity including coordinate/frame/sequence/BTOP field-only fixtures, and ignored subject/DB pairwise, XML, plus identity-field repro fixtures for the remaining complex TBLASTX frame-shift rendering gaps, including NCBI-only short HSPs in XML and identity-field output | Needs broader upstream fixture parity for complex TBLASTX frame-shift traceback rendering |
| `s_OutOfFrameGappedAlign` | blast_gapalign.c:1950 | Represented by `s_out_of_frame_gapped_align`; traceback dispatch and score-only three-frame OOF DP score-state recurrence live directly in the name-matched Rust function, including score-only and traceback empty-input sentinel offsets | Needs TBLASTX/BLASTX fixture parity for final traceback rendering |
| `s_OutOfFrameSemiGappedAlignWrap` | blast_gapalign.c:4223 | Represented by `s_out_of_frame_semi_gapped_align_wrap`; BLASTX switch-sequence routing including score-only and traceback swapped dispatch plus switched and non-switched empty-input sentinel offset mapping, and direct TBLASTN non-switch score-only and traceback dispatch are covered by focused tests | Needs TBLASTX/BLASTX fixture parity |
| `s_BlastOOFTracebackToGapEditScript` | blast_gapalign.c:4451 | Represented by `s_blast_oof_traceback_to_gap_edit_script`; required-input rejection, empty-traceback and non-positive-length no-script output, shifted-substitution propagation, left/right merge, in-frame-gap flow-through at the left/right boundary, opposing left/right frame-shift cancellation, same-direction frame-shift combination with right-extension substitution preservation, nucleotide-length truncation including exact-boundary stopping and truncation inside multi-count frame-shift spans, frame-shift splitting, and substitution post-adjustment after split insertion/deletion frame-shifts are present | Needs fixture parity against C for end-to-end complex frame-shift scripts |

CCC currently reports no unmapped upstream functions in the tracked core set.
